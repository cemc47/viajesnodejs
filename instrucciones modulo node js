Instalar extensiones en vscode

ES7 React/Redux/GraphQL/React-Native snippets
Simple React Snippets

Usar template
https://cdnjs.com/
- skeleton
- 

	
TIDevEx18.65

 
 
 Node JS
 Sequelize ORM
 
 cd introNodeJS
 npm init
prueba de datos fsfs
 --- definir todas las variables
 npm install --save nodemon body-parser express
 
		 body-parser, requerido para presentar ciertas cosas
		 express, es el servidor
		 nodemon, detecta cambios y los refleja (como liveserver)
 
 npm intall, toma el archivo package.json e instala todas las dependencias
 
 
 npm run start // iniciar servidor
 
 ctr + c para detener el servidor
 
 npm install --save pug
 
 https://html2jade.org/
 https://fonts.google.com/
 
 
 npm start env --production   (para iniciar el ambiente de produccion)
 
 
npm install --save mysql2 sequelize

npm install --save-dev dotenv

5a8ddc97
viajesnodejs

// crear app heroku
heroku create --remote production

luego cargar de git a heroku
git push production master

respaldar BD

mysqldump -u root -p agenciadeviajes > agenciadeviajes.sql

// saber informacion de la BD
heroku config |  grep CLEARDB_DATABASE_URL

 pasar base de datos

Get-Content agenciadeviajes.sql | mysql -u b4116be23cc38a -h us-cdbr-east-06.cleardb.net -p heroku_9e05db16301dd90
Get-Content agenciadeviajes.sql | mysql -u b4116be23cc38a -h us-cdbr-east-06.cleardb.net -p heroku_9e05db16301dd90


-- actualziar archivos
git add .
git commit -m "actualziar"
git push origin master
git push production master    ///// esto pasa a heroku